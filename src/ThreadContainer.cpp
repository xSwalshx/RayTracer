//Local Includes
#include "ThreadContainer.h"

ThreadContainer::ThreadContainer(int _StartX, int _StartY, int _EndX, int _EndY, int _OffsetX, int _OffsetY)
{
  setStartX(_StartX);
  setStartY(_StartY);
  setEndX(_EndX);
  setEndY(_EndY);
  setOffsetX(_OffsetX);
  setOffsetY(_OffsetY);
}

